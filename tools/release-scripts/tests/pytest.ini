[pytest]
# Pytest configuration for release-scripts tests
# This is independent from ragora's pytest configuration

testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts = 
    --strict-markers
    --strict-config
    -v

markers =
    unit: Unit tests
    integration: Integration tests

